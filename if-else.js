// Variables
var A = true;
var B = false;
var C = true;

// Runs first and once
function setup() {
  createCanvas(700, 100); // size
  textSize(26);
}

// Loops forever
function draw() {
  background(255);
  text('Surround me with if-elses!', 10, 40);
}
